DROP PROCEDURE IF EXISTS [RSU].[spAutoGen_UserRecruitCohabbitTypes_DELETE];
GO


/**********************************************************************************************************************
**		Name: [RSU].[spAutoGen_UserRecruitCohabbitTypes_DELETE]
**		Desc: Autogenerated SPROC to CREATE a row for [RSU].[UserRecruitCohabbitTypes]
**		Auth: ANDRES E. SOSA
**		Date: 10/02/2015 (UTC)
***********************************************************************************************************************/
CREATE   Procedure [RSU].[spAutoGen_UserRecruitCohabbitTypes_DELETE]
(
	@UserRecruitCohabbitTypeID int
)
AS
BEGIN
	/**************
	 * INITIALIZE
	 **************/
	 DECLARE @ACLUserID UNIQUEIDENTIFIER = CAST(CONTEXT_INFO() AS UNIQUEIDENTIFIER)
		, @UserGUID NVARCHAR(MAX)
		, @CreatedAccessId SMALLINT;

	/*********************
	 * CHECK ACCESS LEVEL
	 *********************/
	 IF (NOT EXISTS(SELECT * FROM [GEN].fxGetAccessLevel('DELETE','RSU','UserRecruitCohabbitTypes') AS AL WHERE (AL.DeleteAccessId > 0))) BEGIN
		--** Get ACL Information
		--SELECT @ACLUserID = UserId, @UserGUID = @UserGUID, @CreatedAccessId = AL.UpdateAccessId  FROM [GEN].fxGetAccessLevel('DELETE','RSU','UserRecruitCohabbitTypes') AS AL WHERE (AL.UpdateAccessId > 0)

		--** Check that there is a user
		SELECT TOP 1 @UserGUID = UserGuidIDMasked FROM [GEN].fxGetUserInfo();

		RAISERROR (N'[50130]|The user "%s" does not have DELETE privileges on table "%s".'
           , 11 -- Severity,
           , 1 -- State,
           , @UserGUID
		   , N'[RSU].[UserRecruitCohabbitTypes]');
		RETURN;
	 END

	 /************
	 * UPDATE ROW
	 ************/
	 UPDATE RSU.UserRecruitCohabbitTypes SET
		IsDeleted = 1
		, ModifiedDate = SYSDATETIMEOFFSET()
		, ModifiedById = @ACLUserID
	 
	 WHERE
		(UserRecruitCohabbitTypeID = @UserRecruitCohabbitTypeID);


	 /**************
	 * RETURN ROW
	 **************/
	 SELECT * FROM RSU.UserRecruitCohabbitTypes
	 WHERE
		(UserRecruitCohabbitTypeID = @UserRecruitCohabbitTypeID);

END;

GO
