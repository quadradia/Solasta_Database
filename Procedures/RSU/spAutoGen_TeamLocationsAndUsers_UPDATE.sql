DROP PROCEDURE IF EXISTS [RSU].[spAutoGen_TeamLocationsAndUsers_UPDATE];
GO


/**********************************************************************************************************************
**		Name: [RSU].[spAutoGen_TeamLocationsAndUsers_UPDATE]
**		Desc: Autogenerated SPROC to UPDATE a row for [RSU].[TeamLocationsAndUsers]
**		Auth: ANDRES E. SOSA
**		Date: 10/02/2015 (UTC)
***********************************************************************************************************************/
CREATE   Procedure [RSU].[spAutoGen_TeamLocationsAndUsers_UPDATE]
(
		@TeamLocationsAndUserID INT		
		, @TeamLocationId INT		
		, @UserRecruitId INT		
		, @LocationRoleId INT		
		, @IsActive BIT		
		, @ModifiedDate DATETIMEOFFSET		
		, @ModifiedById UNIQUEIDENTIFIER		
		, @CreatedDate DATETIMEOFFSET		
		, @CreatedById UNIQUEIDENTIFIER
)
AS
BEGIN
	/**************
	 * INITIALIZE
	 **************/
	 DECLARE @ACLUserID UNIQUEIDENTIFIER = CAST(CONTEXT_INFO() AS UNIQUEIDENTIFIER)
		, @UserGUID NVARCHAR(MAX)
		, @CreatedAccessId SMALLINT;

	/*********************
	 * CHECK ACCESS LEVEL
	 *********************/
	 IF (NOT EXISTS(SELECT * FROM [GEN].fxGetAccessLevel('UPDATE','RSU','TeamLocationsAndUsers') AS AL WHERE (AL.UpdateAccessId > 0))) BEGIN
		--** Get ACL Information
		--SELECT @ACLUserID = UserId, @UserGUID = @UserGUID, @CreatedAccessId = AL.UpdateAccessId  FROM [GEN].fxGetAccessLevel('UPDATE','RSU','TeamLocationsAndUsers') AS AL WHERE (AL.UpdateAccessId > 0)

		--** Check that there is a user
		SELECT TOP 1 @UserGUID = UserGuidIDMasked FROM [GEN].fxGetUserInfo();

		RAISERROR (N'[50120]|The user "%s" does not have UPDATE privileges on table "%s".'
           , 18 -- Severity,
           , 1 -- State,
           , @UserGUID
		   , N'[RSU].[TeamLocationsAndUsers]');
		RETURN;
	 END

	 /************
	 * UPDATE ROW
	 ************/
	 UPDATE RSU.TeamLocationsAndUsers SET
		[TeamLocationId] = @TeamLocationId
		, [UserRecruitId] = @UserRecruitId
		, [LocationRoleId] = @LocationRoleId
		, [IsActive] = @IsActive
		, [ModifiedDate] = SYSDATETIMEOFFSET()
		, [ModifiedById] = UTL.fxGetDefaultGuidId(@ModifiedById)
	 WHERE
		(TeamLocationsAndUserID = @TeamLocationsAndUserID);

	 /**************
	 * RETURN ROW
	 **************/
	 SELECT * FROM RSU.TeamLocationsAndUsers
	 WHERE
		(TeamLocationsAndUserID = @TeamLocationsAndUserID);

END;

GO
